@page "/"
@inject NavigationManager NavigationManager

<PageTitle>Home</PageTitle>

<h1>@L["Common.ComingSoon"]</h1>

@code {
    protected override void OnInitialized()
    {
        // Navigate immediately to /home
        NavigationManager.NavigateTo("/home", true);
    }
}